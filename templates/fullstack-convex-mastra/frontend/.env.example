# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to git!
# =============================================================================

# -----------------------------------------------------------------------------
# CONVEX DATABASE
# -----------------------------------------------------------------------------
# Local development: http://localhost:3210 (run: npx convex dev)
# Production: http://lucidlabs-convex:3210
NEXT_PUBLIC_CONVEX_URL=http://localhost:3210

# -----------------------------------------------------------------------------
# LLM CONFIGURATION
# -----------------------------------------------------------------------------
# Local development (Ollama):
#   LLM_BASE_URL=http://localhost:11434/v1
#   LLM_MODEL=phi3:mini
#   LLM_PROVIDER=ollama
#
# Production (Portkey → Claude):
#   LLM_BASE_URL=http://lucidlabs-portkey:8787/v1
#   LLM_MODEL=claude-sonnet-4-5-20250514
#   LLM_PROVIDER=anthropic

LLM_BASE_URL=http://localhost:11434/v1
LLM_MODEL=phi3:mini
LLM_PROVIDER=ollama

# -----------------------------------------------------------------------------
# MASTRA (AI Agent Layer)
# -----------------------------------------------------------------------------
# Local: http://localhost:4000
# Production: http://invoice-mastra:4000
NEXT_PUBLIC_MASTRA_URL=http://localhost:4000

# -----------------------------------------------------------------------------
# AUTHENTICATION (Better Auth)
# -----------------------------------------------------------------------------
# Set to "true" to enable auth (production)
# Set to "false" for offline development without auth
NEXT_PUBLIC_AUTH_ENABLED=false

# Convex Site URL (for auth callbacks)
# NEXT_PUBLIC_CONVEX_SITE_URL=https://your-project.convex.site

# Better Auth Secret (generate with: openssl rand -base64 32)
# BETTER_AUTH_SECRET=your-secret-here

# Site URL for auth redirects
# SITE_URL=http://localhost:3000
